// let amount = "100"; //s start as string.
let amount = 1; //s start as a #number.
// let amount = 99.5; //s Decimal
// let amount = "hello";

//* String to #number
// amount = parseInt(amount);
// amount = +amount;
// amount = Number(amount);

//* #number to String
// amount = amount.toString();
// amount = String(amount);

//* String to Decimal
// amount = parseFloat(amount);

//* #number to Boolean
amount = Boolean(amount);

const logOut = amount;

console.log(logOut, typeof logOut);
